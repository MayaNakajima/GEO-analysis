# GEO 定点観測 分析アプリ（GEO-analysis）

株式会社オンワードコーポレートデザイン｜生成AI出現モニタリングの **row データ深掘り・突合分析** アプリ。
定点観測アプリ（`monitoring`）が出力した `results_*.csv` を読み込み、
**「なぜ出ないのか・何を直すか」** を掘り下げる自己完結型 HTML ダッシュボードを 1 枚生成します。

> 位置づけ：改善案 `ダッシュボード改善案_20260728.md` §3「rowデータ×スクレイピング分析アプリ」の実装。

---

## 内蔵ダッシュボードとの役割分担

|  | 内蔵ダッシュボード（`monitoring/src/dashboard.py`） | 本アプリ（GEO-analysis） |
|---|---|---|
| 目的 | 日々の定点監視・軽量 | 回答全文の深掘り・突合分析 |
| 入力 | `reports/*.json`（集計済み） | `results/*.csv`（回答全文・生データ） |
| 見るもの | 出現率・推移・崖・ブレ | 回答本文／競合共起／引用URL／自社突合 |
| ネット接続 | 不要 | 不要（P3 のみ将来スクレイピング） |
| 一言 | 「今どうなっているか」 | 「なぜそうなのか・何を直すか」 |

内蔵側で「崖は D2」と分かったら、本アプリで「D2 で誰に負けているか・自社実績ページはあるか」を掘る、という導線です。

---

## 使い方

Python 標準ライブラリのみで動作します（追加インストール不要）。実行は **Anaconda Prompt** から。

```bat
REM 1) config.json のパスを自分の環境に合わせて確認
REM 2) フォルダへ移動して生成
cd /d C:\Users\612316\Documents\GitHub\GEO-analysis
python generate.py

REM パスを直接指定する場合（1行）
python generate.py --results-dir "C:\Users\612316\Downloads\06_GEO\monitoring\data\results" --reference "C:\Users\612316\Downloads\06_GEO\Set2_実績スクレイピング_reference.md" --out "C:\Users\612316\Documents\GitHub\GEO-analysis\analysis.html"
```

> 実行手順の詳細（Anaconda Prompt の起動・パス一覧・トラブルシューティング）は `docs/実行手順書_v1.md` を参照。

生成された `analysis.html` をブラウザで開くだけで動きます（外部依存は CDN の Chart.js のみ）。
CSV を再測定・追加したら `python generate.py` を再実行すれば最新データで更新されます。

### config.json

```json
{
  "results_dir": "C:\\Users\\612316\\Downloads\\06_GEO\\monitoring\\data\\results",
  "reference_md": "C:\\Users\\612316\\Downloads\\06_GEO\\Set2_実績スクレイピング_reference.md",
  "output_html": "C:\\Users\\612316\\Documents\\GitHub\\GEO-analysis\\analysis.html"
}
```

> 定点観測アプリ（monitoring）は別リポジトリ・別開発環境にあり、今後修正の可能性があります。
> 本アプリは **同じ CSV を読むだけの疎結合** なので、monitoring 側の改修と独立に運用できます。
> monitoring の場所が変わったら config.json のパスだけ直してください。

---

## 実装している分析（改善案 §3-2 の優先度順）

### P1｜競合共起分析（実装済み）
当社が出なかった回答（`mention_detected=False`）の本文から、代わりに挙がっている **競合企業名を抽出・ランキング**。
ドメイン別に「どの土俵で誰に負けているか」を可視化。行クリックで **根拠の回答全文へドリルダウン**。

### P1｜多軸クロス集計 ＋ ドリルダウン（実装済み）
行軸・列軸を 回(run)／タイミング／ドメイン／特異度ティア／質問タイプ／ステークホルダー／モデル／実行日／質問セット から選び、出現率のクロス表を作成。
セルクリックで該当質問・回答全文の一覧を展開。Set2 の特異度カーブから **崖の位置（D1→D2 等）を自動で言語化**。

### 回(run)/タイミング粒度・過去回比較（実装済み）
定点観測の複数回を、**全回まとめ／回ごと**（同日の r1/r2 も分離）で切替。「過去回比較」タブで 2 回（A/B）を選び、
出現率デルタ・競合の増減・質問の反転（出た↔消えた）・回答差分を確認（r1↔r2 でブレ確認も可）。

### P2｜引用元URL分析（実装済み・データ待ち）
ヒット回答の `urls_found` を集計。稼働モデルが Claude 単体（非グラウンディング）の現状では引用が発生しないため空表示。
Perplexity 等の検索引用型モデルのキー許可後に自動で有効化されます。

### P2｜自社サイト突合（実装済み）
`Set2_実績スクレイピング_reference.md`（自社サイトの掲載顧客・事例）を初期データに、AI 回答本文での言及有無を突合。
**「掲載はあるが AI 未言及」＝ GEO 強化余地** をドメイン単位で可視化。

### P3｜競合サイト突合（将来）
AI 回答に実際に登場した競合サイトのみを対象に取得し、被引用ギャップを比較。
ネット接続・取得制限への配慮が必要なため、オフライン自己完結の本体とは別工程で追加予定。

---

## 競合抽出のロジック（透明性のための注記）

競合名は **回答本文の太字表記（`**社名**`）** を主対象に、以下のヒューリスティックで抽出しています。

- 企業サフィックス（株式会社・ホールディングス・工業 等）があれば企業とみなす
- 単一トークンの固有名詞（漢字/カナ/英字）で 2〜24 文字のものを候補に
- 句読点・記号を含む説明句、一般語（実績・ポイント・機能 等）、当社・自社ブランド（オンワード／Raffiria 等）、
  reference.md の既知の掲載顧客（＝競合ではなくエンドクライアント）は自動除外

**あくまで候補（要目視確認）** です。最終判断は各行のドリルダウンで回答全文を確認してください。

---

## 前提（重要）

稼働モデルは **Claude 単体＝非グラウンディング**。測定対象は「AI の学習記憶に当社があるか」。
現状の実測では出現はほぼ **指名質問（D1）に限定** され、業種・実績質問（D2）以下は 0%＝ **崖は D1→D2**。
この構造を掘り下げるのが本アプリの目的です。

## ファイル構成

```
GEO-analysis/
├─ generate.py     … データ処理＋HTML生成（stdlibのみ）
├─ config.json     … データパス設定
├─ analysis.html   … 生成物（.gitignore 対象・再生成可能）
├─ README.md
└─ .gitignore
```
